<!DOCTYPE html>
<html>
  <head>
    <title>FriendApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <ul>
      <% if logged_in? %>
        <li><%= link_to "Profile", user_path(@current_user.id)%></li>
        <li><%= link_to "Logout", session_path(@current_user.id), method: :delete %></li>
        <li><%= link_to "投稿をする", new_feed_path %></li>
        <li><%= link_to "投稿一覧", feeds_path %></li>
      <% else %>
        <li><%= link_to "Sing up", new_user_path %></li>
        <li><%= link_to "Login", new_session_path %></li>
      <% end %>
    </ul>
    <% flash.each do |key, value| %>
    <%= content_tag(:div,value,class:"#{key}")%>
    <% end %>
    </header>
    <%= yield %>
    <footer>
      <p>運営会社 友だちとつながる 株式会社</p>
      <p>© 2021- Connect with Friends Corp.</p>
    </footer>
  </body>
</html>
